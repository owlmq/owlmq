TODO:
* fingertable befüllen wie im paper beschrieben
* configfile einbauen
    * entscheiden ob nur in memory oder auch auf die platte geschrieben wird
* key-value paare verschieben wenn ein knoten das cluster verlässt
* replication auf die nachfolgenden knoten, bei ausfall des vorgängers auf die gewünschte menge an kopien ergänzen
